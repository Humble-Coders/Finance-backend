[pytest]
asyncio_default_fixture_loop_scope = function
markers =
    integration: needs a real Postgres (skipped when DATABASE_URL is unset).
        Constraint behaviour cannot be proven without one — SQLite does not
        implement Postgres UUIDs, enums or partial indexes. Backend CI (#13)
        runs without a database, so these skip there; ticket #15 turns them on.
